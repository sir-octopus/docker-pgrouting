services:
  pgrouting:
    image: pgrouting/pgrouting:18-3.6-4.0
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql
    environment:
      - POSTGRES_PASSWORD=postgres
volumes:
  db-data:
